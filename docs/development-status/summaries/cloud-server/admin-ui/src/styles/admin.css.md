# `cloud-server/admin-ui/src/styles/admin.css`

## 功能
Admin UI 全局样式表。暗色主题 CSS 自定义属性、布局（侧导航+主内容）、表单、表格、徽章、卡片、图表容器、登录页。**v5: 新增 `.admin-key-code`/`.admin-key-ft`/`.admin-input-xs`（per-key 容错 UI）、`.admin-info-grid`/`.admin-info-item`/`.admin-info-label`/`.admin-info-value`（设置页信息网格）、`.admin-success`/`.admin-mt`（设置页反馈）。v12: 新增 `.admin-key-created-banner`/`.admin-key-created-header`/`.admin-key-created-name`/`.admin-key-created-value`（API Key 创建成功横幅 + 复制按钮）。**

## 关系
### 被依赖
- `cloud-server/admin-ui/src/main.tsx` / `index.html`
