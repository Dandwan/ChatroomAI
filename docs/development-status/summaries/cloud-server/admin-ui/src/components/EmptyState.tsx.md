# `cloud-server/admin-ui/src/components/EmptyState.tsx`

## 功能
图形化空状态组件（v16 新增）。虚线方框图标 + 标题 + 描述 + 可选操作按钮，替换列表为空时的裸文字。样式由 admin.css 的 `.admin-empty-mark`/`.admin-empty-title`/`.admin-empty-desc` 提供。

## 关系
### 提供
- `EmptyState` — 空状态组件（props: title / desc / action）

### 被依赖
- `cloud-server/admin-ui/src/pages/ApiKeysPage.tsx`

## 关键词
### 函数
- `EmptyState`
